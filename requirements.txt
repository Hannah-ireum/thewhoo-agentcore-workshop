# Runtime
# 하한 = 최초 완주 검증 버전(2026-05-13). 상한 = major 경계 — pip 이 breaking change 가
# 있는 major 를 끌어오는 것만 차단하고, minor/patch 는 최신을 받도록 열어 둡니다.
#
# 주의: 상한이 major 경계이므로 실제 설치 버전은 시간이 지나면 하한보다 훨씬 위입니다.
# 2026-08-19 시점 clean install 해석 결과 — strands-agents 1.52.0 /
# bedrock-agentcore 1.22.0 / bedrock-agentcore-starter-toolkit 0.3.11 /
# strands-agents-evals 0.3.0 / mcp 1.29.0 / boto3 1.43.74.
# 이 조합으로 import·Memory 4-strategy·Evaluate·CLI 플래그 호환성을 재확인했습니다.
# 워크샵 직전에는 새 venv 로 clean install 후 Lab 1·2·5·8 을 한 번 돌려보는 것을 권장합니다.
strands-agents>=1.39.0,<2.0
strands-agents-tools>=0.5.2,<1.0
strands-agents-evals>=0.1.17,<1.0
bedrock-agentcore>=1.9.1,<2.0
bedrock-agentcore-starter-toolkit>=0.3.9,<1.0
boto3>=1.43.0,<2.0
botocore>=1.43.0,<2.0
mcp>=1.27.0,<2.0
requests-aws4auth>=1.3.0,<2.0

# UI
streamlit>=1.35.0,<2.0

# Observability (AgentCore Runtime OTEL)
# aws-opentelemetry-distro 가 auto-instrumentation 을 제공합니다.
# 빠지면 Strands 내부 LLM/tool span 이 X-Ray 로 안 흘러갑니다.
#
# app.py 의 StrandsTelemetry().setup_otlp_exporter() 는 구현상
# opentelemetry.exporter.otlp.proto.**http** 를 씁니다 (실측: strands 1.52.0
# 소스 확인). distro 가 http·grpc·sdk 를 == 로 함께 고정해 끌어오지만,
# 전이 의존에만 기대면 distro 버전이 바뀔 때 조용히 깨집니다.
# 그래서 http exporter 를 명시합니다.
aws-opentelemetry-distro>=0.10.0,<1.0
opentelemetry-sdk>=1.24.0,<2.0
opentelemetry-exporter-otlp-proto-http>=1.24.0,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0,<2.0

# Utilities
python-dotenv>=1.0.0,<2.0
pydantic>=2.7.0,<3.0
