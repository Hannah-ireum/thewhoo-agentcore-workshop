# 시작하기 전에

> **왜 AgentCore인가?** Amazon Bedrock AgentCore는 모든 프레임워크·모델과 호환되는 프로덕션 에이전트 플랫폼입니다. AWS는 기존 Amazon Bedrock Agents를 계속 지원하지만, 신규 구축에는 AgentCore + Strands Agents 조합을 권장합니다. 이 워크샵은 처음부터 AgentCore로 구축합니다.

## 실습 환경

Workshop Studio 가 제공하는 임시 AWS 계정에서 진행합니다.

* **리전**: `us-east-1`
* **IDE**: SageMaker Studio Code Editor (Python 3.11, 5GB)
* **언어 / 프레임워크**: Python 3.11+, Strands Agents SDK, AgentCore SDK

## 사용하는 AWS 서비스

| 서비스 | 역할 |
|---|---|
| Amazon Bedrock | 언어 모델 (Claude) + Knowledge Base |
| Amazon Bedrock AgentCore | Runtime, Memory, Gateway, Observability, Evaluations |
| Amazon S3 / S3 Vectors | 샘플 데이터 저장 + 벡터 스토어 |
| AWS Lambda | Mock 상품 검색·추천·재고·프로모션 API |
| Amazon CloudWatch | 에이전트 로그·트레이스·대시보드 |
| AWS IAM | 권한 관리 |

## 1단계. Workshop Studio 접속

이벤트 링크로 접속하면 임시 AWS Console 이 열립니다. 리전이 `us-east-1` 인지 우측 상단에서 확인하세요.

## 2단계. Bedrock 모델 access 확인

> **서버리스 foundation model 은 별도 승인 절차 없이 기본 활성화**됩니다. 공식 문서: *"Access to all Amazon Bedrock foundation models is enabled by default with the correct AWS Marketplace permissions in all commercial AWS Regions."* 처음 호출할 때 Bedrock 이 background 로 subscription 을 자동 진행합니다.
>
> 단, 아래 두 가지 전제가 충족돼야 합니다 ([공식 문서](https://docs.aws.amazon.com/bedrock/latest/userguide/model-access.html)).

| 전제 | 내용 |
|---|---|
| **AWS Marketplace 권한** | IAM role 에 `aws-marketplace:Subscribe` · `Unsubscribe` · `ViewSubscriptions` 필요. 최초 1회만 쓰이고, 활성화 후에는 없어도 호출됩니다 |
| **Anthropic FTU 양식** | Anthropic 모델은 **호출 전에 First Time Use 양식 제출이 필수**입니다 (계정 또는 조직 관리계정당 1회) |

> ⚠️ **Anthropic FTU 는 "혹시" 가 아니라 필수 단계입니다.** 공식 문서 원문: *"For Anthropic models, you must complete the First Time Use (FTU) form before invoking the model."*
>
> 이 워크샵은 Claude 모델을 쓰므로, **Pre-Lab 전에 미리 처리해 두는 것을 권장**합니다. 워크샵 중에 막히면 전체 진행이 멈춥니다.
>
> **제출 방법 (둘 중 하나)**
> - Console → Amazon Bedrock → **Model catalog** → Anthropic 모델 선택 → use case 양식 제출
> - API: `PutUseCaseForModelAccess` (양식 제출 즉시 access 부여)
>
> 양식에는 사용 목적과 웹사이트 URL 이 필요합니다. 개인 개발자라면 포트폴리오·GitHub 프로필 URL 도 인정됩니다.
> 조직 관리계정에서 제출하면 같은 AWS Organization 의 다른 계정이 상속받습니다.
>
> `grant-sagemaker-permissions.sh` 가 Marketplace 권한 부여와 모델 1회 호출(자동 활성화 유도)을 수행하지만, **FTU 양식은 사람이 제출해야 합니다.**

이 워크샵이 사용하는 모델:

- **Claude Sonnet 4.6** — Orchestrator
- **Claude Haiku 4.5** — 서브 Agent + LLM-as-judge
- **Titan Embed Text v2** — Knowledge Base 임베딩

### `AccessDeniedException` 이 Lab 중에 나올 경우

공식 문서가 설명하는 타이밍 이슈입니다 — 첫 호출 시 subscription 이 진행되는 **최대 15분** 동안은 호출이 일시적으로 성공하다가, 전제가 빠져 있으면 이후 `AccessDeniedException` 이 나옵니다. 권한을 부여한 뒤에도 subscription 완료까지 **최대 2분** 이 더 걸릴 수 있습니다.

체크 순서:

1. **Anthropic FTU 양식을 제출했는지** (가장 흔한 원인)
2. IAM role 에 `aws-marketplace:Subscribe` 가 있는지
3. 계정에 유효한 결제 수단이 등록돼 있는지
4. 위가 모두 맞으면 2분 정도 기다린 뒤 재시도

현재 활성 여부는 이 명령으로 확인할 수 있습니다.

```bash
aws bedrock get-foundation-model-availability \
  --model-id anthropic.claude-haiku-4-5-20251001-v1:0 \
  --region us-east-1
```

`agreementAvailability.status` 가 `AVAILABLE` 이면 정상입니다. (AWS CLI 2.27.42 이상 필요)
## 3단계. Pre-Lab 으로 이동

[**Pre-Lab. 인프라 셋업**](00-pre-lab-셋업.md) 에서 스크립트 3개로 인프라 + Python 환경을 준비합니다. 약 15분.

## 워크샵 전체 구조

![최종 아키텍처](assets/diagrams/final.svg)

Lab 을 따라가면서 박스 하나씩 채워집니다.

---

준비됐으면 [**Pre-Lab**](00-pre-lab-셋업.md) 으로 이동하세요.
